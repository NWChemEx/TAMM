Copy the contents of folder ctce_v6.5 in the nwchem 
src/tce directory to use the tamm library. 
Neessary Fortran patchfiles are included here.


To build the tamm library - cmake, flex and a blas library are needed.

mkdir build && cd build

#Specify blas header,lib paths to cmake along with an install prefix as shown

cmake .. -DTAMM_BLAS_INC=/usr/include -DTAMM_BLAS_LIB=/usr/lib -DTAMM_INSTALL_PREFIX=/home/user/tamm-install


